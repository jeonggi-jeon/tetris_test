/** 보드: 가시 영역 + 상단 버퍼 (회전 여유) */
export const COLS = 10;
export const VISIBLE_ROWS = 20;
export const BUFFER_ROWS = 2;
export const ROWS = VISIBLE_ROWS + BUFFER_ROWS;

/**
 * 각 테트로미노의 4회전. 셀은 4×4 로컬 격자 내 [row, col] 오프셋.
 */
export const SHAPES = {
  I: [
    [[1, 0], [1, 1], [1, 2], [1, 3]],
    [[0, 2], [1, 2], [2, 2], [3, 2]],
    [[2, 0], [2, 1], [2, 2], [2, 3]],
    [[0, 1], [1, 1], [2, 1], [3, 1]],
  ],
  O: [
    [[0, 1], [0, 2], [1, 1], [1, 2]],
    [[0, 1], [0, 2], [1, 1], [1, 2]],
    [[0, 1], [0, 2], [1, 1], [1, 2]],
    [[0, 1], [0, 2], [1, 1], [1, 2]],
  ],
  T: [
    [[0, 1], [1, 0], [1, 1], [1, 2]],
    [[0, 1], [1, 1], [1, 2], [2, 1]],
    [[1, 0], [1, 1], [1, 2], [2, 1]],
    [[0, 1], [1, 0], [1, 1], [2, 1]],
  ],
  S: [
    [[0, 1], [0, 2], [1, 0], [1, 1]],
    [[0, 1], [1, 1], [1, 2], [2, 2]],
    [[1, 1], [1, 2], [2, 0], [2, 1]],
    [[0, 0], [1, 0], [1, 1], [2, 1]],
  ],
  Z: [
    [[0, 0], [0, 1], [1, 1], [1, 2]],
    [[0, 2], [1, 1], [1, 2], [2, 1]],
    [[1, 0], [1, 1], [2, 1], [2, 2]],
    [[0, 1], [1, 0], [1, 1], [2, 0]],
  ],
  J: [
    [[0, 0], [1, 0], [1, 1], [1, 2]],
    [[0, 1], [0, 2], [1, 1], [2, 1]],
    [[1, 0], [1, 1], [1, 2], [2, 2]],
    [[0, 1], [1, 1], [2, 0], [2, 1]],
  ],
  L: [
    [[0, 2], [1, 0], [1, 1], [1, 2]],
    [[0, 1], [1, 1], [2, 1], [2, 2]],
    [[1, 0], [1, 1], [1, 2], [2, 0]],
    [[0, 0], [0, 1], [1, 1], [2, 1]],
  ],
};

export const PIECE_TYPES = Object.keys(SHAPES);

/** 메인 보드용 그라데이션·글로우 색 (Canvas) */
export const COLORS = {
  I: { core: "#6ff7ff", glow: "#00e5ff" },
  O: { core: "#ffe566", glow: "#ffcc00" },
  T: { core: "#d494ff", glow: "#b366ff" },
  S: { core: "#7cff9a", glow: "#00ff88" },
  Z: { core: "#ff7b8a", glow: "#ff3366" },
  J: { core: "#7aa8ff", glow: "#4488ff" },
  L: { core: "#ffb47a", glow: "#ff8844" },
};

/** 점수 (NES 스타일, 레벨 배수는 별도 적용) */
export const LINE_SCORE = [0, 40, 100, 300, 1200];

/** 레벨당 라인 수 */
export const LINES_PER_LEVEL = 10;

/** 초당 프레임 (고정 스텝용) */
export const FPS = 60;
